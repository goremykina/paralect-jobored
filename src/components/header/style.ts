import styled from 'styled-components';

const InnerBlock = styled.div`
  max-width: 65rem;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  margin: 0 auto;
`

const Logo = styled.div`
  display: flex;
  align-items: center;
  position: absolute;
  left: 0;
`

export { InnerBlock, Logo }
